# CLI WeChat Bridge Product Site

This directory contains the standalone Astro site published to GitHub Pages.

```bash
npm install
npm run dev
npm run build
```

The build copies the selected real product screenshots from `../docs/images` into the ignored `public/assets/real/` directory. Tracked visual assets belong in `public/assets/generated/`.
